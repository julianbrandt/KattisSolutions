using System;

namespace AMultiplicationGame
{
    class Program
    {
        static void Main(string[] args)
        {
            string line;
            while ((line = Console.ReadLine()) != null)
            {
                var number = long.Parse(line);
                long sum = 1;
                var person = 1;

                while (sum < number)
                {
                    sum *= 
                }
            }
        }
    }
}